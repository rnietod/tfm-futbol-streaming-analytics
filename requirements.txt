# TactixData — dependencias DIRECTAS del backend (Python 3.11+).
# Set mínimo que el proyecto importa; pip resuelve las transitivas.
# Para reproducir el entorno exacto usa requirements-lock.txt.
#
#   python -m venv venvfutbol
#   . venvfutbol/Scripts/activate          # Windows (PowerShell: venvfutbol\Scripts\Activate.ps1)
#   pip install -r requirements.txt

# --- API / WebSocket ---
fastapi==0.127.0
uvicorn==0.40.0
websockets==15.0.1

# --- Persistencia / caché ---
SQLAlchemy==2.0.45
pg8000==1.31.5            # driver PostgreSQL puro-Python (sin libpq)
redis==7.1.0

# --- Datos / analítica ---
pandas==2.3.3
pyarrow==21.0.0          # requerido por BigQuery .to_dataframe()

# --- Google Cloud (warehouse BigQuery + fotos de jugadores en GCS) ---
google-cloud-bigquery==3.40.1
google-cloud-storage==3.6.0
google-api-core==2.30.0
db-dtypes==1.5.0         # mapeo de tipos BigQuery -> pandas

# --- Dashboard de control / simulación (Streamlit) ---
streamlit==1.51.0
requests==2.32.5

# --- Generación del mapping opta <-> tracking (scripts/data) ---
thefuzz==0.22.1
Unidecode==1.4.0

# --- Desarrollo / QA ---
pytest==9.0.1
pytest-mock==3.15.1
flake8==7.3.0
